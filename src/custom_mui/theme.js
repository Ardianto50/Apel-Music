import { createTheme } from "@mui/material";


const theme = createTheme({
    typography: {
        allVariants: {
            fontFamily: 'poppins',
            textTransform: 'none',
        }
    }
});

export default theme;